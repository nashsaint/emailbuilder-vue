export function renderTableTree(tree) {
// Placeholder: a table-based renderer for better email client support.
// Keep API compatible with renderDivTree(tree).
return '<!-- TODO: implement table-based renderer -->'
}